#!/bin/bash
set -e

# Basic system name
echo "lemonzos" > /etc/hostname

# Update the OS name in the settings
sed -i 's/PRETTY_NAME=.*/PRETTY_NAME="LemonzOS"/' /etc/os-release

echo "Post-install script finished successfully."
