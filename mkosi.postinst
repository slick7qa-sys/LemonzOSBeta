#!/bin/sh
set -e

# Create a user named lemon with password lemon
if ! id "lemon" >/dev/null 2>&1; then
    useradd -m -G sudo -s /bin/bash lemon
    echo "lemon:lemon" | chpasswd
    echo "root:lemon" | chpasswd
fi

# Ensure the system knows to start the GUI
systemctl set-default graphical.target
